{
  "name": "crewdesk",
  "version": "0.1.0",
  "description": "A local dashboard for Claude Code: every project, every agent, what stage the work is in, and how much of your token window is left.",
  "type": "module",
  "bin": {
    "crewdesk": "bin/crewdesk.mjs"
  },
  "main": "./src/server.mjs",
  "files": [
    "bin/",
    "src/",
    "public/",
    "demo/",
    "hooks/",
    "README.md",
    "LICENSE",
    "LICENSE-THIRD-PARTY.md"
  ],
  "scripts": {
    "start": "node bin/crewdesk.mjs",
    "dev": "node bin/crewdesk.mjs --port 4601",
    "check": "node -e \"const{readdirSync}=require('node:fs');const{join}=require('node:path');const{spawnSync}=require('node:child_process');const skip=new Set(['node_modules','.git','assets']);let bad=0;const walk=(d)=>{for(const e of readdirSync(d,{withFileTypes:true})){if(skip.has(e.name))continue;const p=join(d,e.name);if(e.isDirectory())walk(p);else if(/[.]m?js$/.test(e.name)){const r=spawnSync(process.execPath,['--check',p],{encoding:'utf8'});if(r.status!==0){bad++;console.error(p+': '+(r.stderr||'').trim().split('\\n').slice(0,3).join(' '));}}}};walk('.');if(bad){console.error(bad+' file(s) failed node --check');process.exit(1)}console.log('check: every .js/.mjs file parses')\"",
    "demo": "node bin/crewdesk.mjs demo"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "claude-code",
    "claude",
    "ai-agents",
    "agent",
    "dashboard",
    "observability",
    "monitoring",
    "kanban",
    "token-usage",
    "pixel-art",
    "developer-tools",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mhmmdglc/crewdesk.git"
  },
  "bugs": {
    "url": "https://github.com/mhmmdglc/crewdesk/issues"
  },
  "homepage": "https://github.com/mhmmdglc/crewdesk#readme",
  "author": "mhmmdglc"
}
